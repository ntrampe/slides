---
alwaysApply: true
---

Follow naming patterns: Components = PascalCase.tsx (PhotoDisplay.tsx), Hooks = use*.ts (useSlideshow.ts), Services/Repos = *Service.ts/*Repo.ts (ImmichPhotoRepo.ts), Hook returns = Use[Name]Return interface, Component props = [Name]Props interface. Always use explicit TypeScript interfaces, never 'any' types.